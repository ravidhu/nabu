# The toolchain contributors develop with. The compatibility promise lives in
# Cargo.toml's `rust-version` (MSRV) — these are different things and pinning an
# exact channel here just leaves developers on a stale compiler.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
targets = ["aarch64-apple-darwin"]
profile = "minimal"
